{
  "buildCommand": "vite build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/:naddr(naddr1[a-z0-9]+)", "destination": "/view/:naddr" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
