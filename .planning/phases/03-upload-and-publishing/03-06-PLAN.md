---
phase: 03-upload-and-publishing
plan: 06
type: execute
wave: 5
depends_on:
  - 03-05
files_modified:
  - src/components/upload/ShareCard.tsx
  - src/components/upload/UploadPanel.tsx
  - src/components/upload/ProgressList.tsx
autonomous: false
requirements:
  - UPLD-07
  - UPLD-08
  - CONF-01
  - CONF-02

must_haves:
  truths:
    - "User can drop photos, watch progress through encrypting and uploading states in the list, then see share link"
    - "Upload button appears only after all photos finish processing (status=done in processing store)"
    - "After clicking Upload, each photo shows encrypting → uploading → done in ProgressList"
    - "Optional album title field appears after all uploads complete and before share link is displayed"
    - "'Publishing to Nostr...' spinner shown while event is being published"
    - "Share link is displayed as large copyable text with copy button only after relay confirms"
    - "SettingsPanel is visible below the drop zone, collapsed by default"
    - "Human verifies the full flow works end-to-end in a real browser"
  artifacts:
    - path: "src/components/upload/ShareCard.tsx"
      provides: "Post-upload share link display with album title field, copy button, spinner"
      contains: "shareLink\|copy"
    - path: "src/components/upload/UploadPanel.tsx"
      provides: "Wired with useUpload, useSettings, SettingsPanel, ShareCard"
      contains: "useUpload\|useSettings\|SettingsPanel\|ShareCard"
    - path: "src/components/upload/ProgressList.tsx"
      provides: "StatusDot extended to handle encrypting and uploading states"
      contains: "encrypting\|uploading"
  key_links:
    - from: "src/components/upload/UploadPanel.tsx"
      to: "src/hooks/useUpload.ts"
      via: "useUpload() called in UploadPanel; startUpload triggered by Upload button"
      pattern: "useUpload"
    - from: "src/components/upload/UploadPanel.tsx"
      to: "src/hooks/useSettings.ts"
      via: "useSettings() provides settings passed to startUpload"
      pattern: "useSettings"
    - from: "src/components/upload/UploadPanel.tsx"
      to: "src/store/processingStore.ts"
      via: "reads photos to find ProcessedPhoto results to pass to startUpload"
      pattern: "useProcessingStore"
    - from: "src/components/upload/ProgressList.tsx"
      to: "src/store/uploadStore.ts"
      via: "reads upload status after processing status to show unified state"
      pattern: "useUploadStore"
---

<objective>
Wire all Phase 3 pieces into the UploadPanel: add Upload button, ShareCard for post-upload UI, SettingsPanel below the drop zone, and update ProgressList to show encrypting/uploading status states. Then perform human verification of the full end-to-end flow.

Purpose: This is the final wiring that makes Phase 3 functional in the browser. All the logic exists — this plan connects it to the UI and verifies it works with a real Blossom server and Nostr relay.
Output: Updated UploadPanel, new ShareCard, updated ProgressList, human-verified upload flow.
</objective>

<execution_context>
@/Users/flox/.claude/get-shit-done/workflows/execute-plan.md
@/Users/flox/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-upload-and-publishing/03-CONTEXT.md
@.planning/phases/03-upload-and-publishing/03-05-SUMMARY.md

<interfaces>
<!-- All contracts from prior plans needed here. -->

From src/hooks/useUpload.ts (Plan 04 — exists):
```typescript
export interface UploadSettings { blossomServer: string; relays: string[] }
export function useUpload(): {
  startUpload: (photos: ProcessedPhoto[], settings: UploadSettings) => Promise<void>;
  shareLink: string | null;
  isUploading: boolean;
  publishError: string | null;
}
```

From src/hooks/useSettings.ts (Plan 05 — exists):
```typescript
export function useSettings(): {
  relays: string[];
  setRelays: (relays: string[]) => void;
  blossomServer: string;
  setBlossomServer: (url: string) => Promise<void>;
  blossomError: string | null;
  isValidating: boolean;
}
```

From src/store/processingStore.ts (EXISTS):
```typescript
// useProcessingStore((s) => s.photos) → Record<string, PhotoProcessingState>
// photos[id].status === 'done' means photos[id].result (ProcessedPhoto) is available
```

From src/store/uploadStore.ts (Plan 03 — exists):
```typescript
// useUploadStore((s) => s.photos) → Record<string, PhotoUploadState>
// status can be 'encrypting' | 'uploading' | 'done' | 'error'
```

From src/components/upload/SettingsPanel.tsx (Plan 05 — exists):
```typescript
// Props: { settings: UseSettingsReturn }
```

ProgressList currently uses PhotoProcessingStatus in StatusDot colors map.
The type now includes 'encrypting' | 'uploading' — StatusDot must handle them.
ProgressList should merge processing store + upload store to show the latest status per photo.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ShareCard and update ProgressList for upload states</name>
  <files>src/components/upload/ShareCard.tsx, src/components/upload/ProgressList.tsx</files>

  <read_first>
    - src/components/upload/ProgressList.tsx — current StatusDot colors map, PhotoRow component
    - src/store/uploadStore.ts — PhotoUploadState type, status values
    - src/types/processing.ts — PhotoProcessingStatus union (now includes encrypting, uploading)
    - .planning/phases/03-upload-and-publishing/03-CONTEXT.md — LOCKED UX: "Optional album title text field shown after upload completes, before share link is displayed", "Publishing to Nostr... spinner", "Share link displayed as large copyable text + copy button"
  </read_first>

  <action>
    1. Create src/components/upload/ShareCard.tsx:
       - Props: { shareLink: string | null; isUploading: boolean; publishError: string | null; onTitleChange?: (title: string) => void }
       - When isUploading=true AND shareLink=null: show "Publishing to Nostr..." with animate-spin spinner
       - When shareLink is non-null:
         a. Show optional album title input: `<input type="text" placeholder="Album title (optional)" onChange={...} />`
         b. Show share link in a large gray code box (text-sm font-mono break-all)
         c. Show "Copy link" button → calls navigator.clipboard.writeText(window.location.origin + shareLink)
         d. Show green checkmark "Copied!" state for 2 seconds after click (useState)
       - When publishError is non-null: show red error text "Failed to publish: {publishError}"
       - When isUploading=false AND shareLink=null AND publishError=null: return null (nothing to show)
       - Tailwind classes: card-style wrapper (rounded-lg border p-6 bg-white shadow-sm mt-6)

    2. Update src/components/upload/ProgressList.tsx:
       - Import useUploadStore from '@/store/uploadStore'
       - In ProgressList component: also read `uploadPhotos = useUploadStore((s) => s.photos)`
       - For each entry in processingPhotos: if uploadPhotos[photo.id] exists, use uploadPhotos[photo.id].status instead of photo.status for display
       - Update StatusDot colors map to add:
         ```
         encrypting: 'bg-purple-400 animate-pulse',
         uploading: 'bg-yellow-400 animate-pulse',
         ```
       - The status label text in PhotoRow (capitalize) will show "Encrypting" and "Uploading" automatically
       - Keep existing colors for pending, processing, done, error unchanged
  </action>

  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "ShareCard|ProgressList|error" || echo "TypeScript OK"</automated>
  </verify>

  <acceptance_criteria>
    - test -f src/components/upload/ShareCard.tsx — exits 0
    - grep "Publishing to Nostr\|isUploading" src/components/upload/ShareCard.tsx — exits 0
    - grep "navigator\.clipboard\|Copy" src/components/upload/ShareCard.tsx — exits 0
    - grep "encrypting.*bg-\|bg-.*encrypting" src/components/upload/ProgressList.tsx — exits 0
    - grep "uploading.*bg-\|bg-.*uploading" src/components/upload/ProgressList.tsx — exits 0
    - grep "useUploadStore" src/components/upload/ProgressList.tsx — exits 0
    - npx tsc --noEmit exits 0
  </acceptance_criteria>

  <done>ShareCard shows spinner, title field, share link, copy button. ProgressList shows encrypting/uploading states with colored dots. Both compile TypeScript clean.</done>
</task>

<task type="auto">
  <name>Task 2: Wire UploadPanel with useUpload, useSettings, SettingsPanel, ShareCard, and Upload button</name>
  <files>src/components/upload/UploadPanel.tsx</files>

  <read_first>
    - src/components/upload/UploadPanel.tsx — current implementation (DropZone + ProgressList)
    - src/hooks/useUpload.ts — useUpload hook return shape
    - src/hooks/useSettings.ts — useSettings hook return shape
    - src/store/processingStore.ts — useProcessingStore to get processed photos
    - .planning/phases/03-upload-and-publishing/03-CONTEXT.md — LOCKED: "Upload button appears after processing complete", "Collapsible settings below drop zone", "Share link auto-copies to clipboard"
  </read_first>

  <action>
    Update src/components/upload/UploadPanel.tsx:
    - Import and call useUpload() → { startUpload, shareLink, isUploading, publishError }
    - Import and call useSettings() → settings
    - Import useProcessingStore to get processed photos
    - Import SettingsPanel from './SettingsPanel'
    - Import ShareCard from './ShareCard'

    Layout (top to bottom in the max-w-2xl column):
    1. h1 + p (existing)
    2. DropZone (existing — disabled when isUploading)
    3. SettingsPanel (below DropZone, always visible) — pass `settings` prop
    4. ProgressList (after first photo added)
    5. Upload button — shown only when: at least 1 photo AND all photos status='done' in processing store AND !isUploading AND !shareLink
       - Button text: "Upload {n} photos to Nostr"
       - Button onClick: collect ProcessedPhoto[] from processingStore photos where status='done' and result exists; call startUpload(processedPhotos, { blossomServer: settings.blossomServer, relays: settings.relays })
       - Tailwind: `w-full rounded-lg bg-blue-600 px-4 py-3 text-white font-semibold hover:bg-blue-700 mt-4`
    6. ShareCard — shown when isUploading OR shareLink OR publishError

    Upload button visibility logic:
    ```typescript
    const processedPhotos = Object.values(photos).filter((p) => p.status === 'done' && p.result);
    const allDone = processedPhotos.length > 0 && processedPhotos.length === Object.keys(photos).length;
    const showUploadButton = allDone && !isUploading && !shareLink;
    ```

    DropZone disabled prop: pass `disabled={isUploading}` to prevent new drops during upload.
    (Add disabled prop to DropZone.tsx if it doesn't exist — just disable the onClick and show opacity-50)

    Run: `npm run build` to confirm no Next.js build errors.
    Run: `npx vitest run` to confirm all tests still pass.
  </action>

  <verify>
    <automated>npx vitest run && npx tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
    - grep "useUpload" src/components/upload/UploadPanel.tsx — exits 0
    - grep "useSettings" src/components/upload/UploadPanel.tsx — exits 0
    - grep "SettingsPanel" src/components/upload/UploadPanel.tsx — exits 0
    - grep "ShareCard" src/components/upload/UploadPanel.tsx — exits 0
    - grep "Upload.*photos\|startUpload" src/components/upload/UploadPanel.tsx — exits 0
    - npx tsc --noEmit exits 0
    - npx vitest run exits 0 (no regressions)
  </acceptance_criteria>

  <done>UploadPanel wired with all Phase 3 hooks and components. Upload button appears after processing completes. SettingsPanel below drop zone. ShareCard shown after upload.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Full Phase 3 upload pipeline:
    - Blossom upload library (sha256Hex, BUD-11 auth, hash verification)
    - kind 30078 Nostr event builder with NIP-40 expiry
    - Upload store with encrypting/uploading/done/error states
    - useUpload hook (p-limit concurrency, retry with backoff, relay OK gate)
    - useSettings hook + SettingsPanel (localStorage persistence, Blossom CORS validation)
    - ShareCard (spinner, title field, copy button)
    - UploadPanel wired end-to-end
  </what-built>

  <how-to-verify>
    1. Start the dev server: `npm run dev` — visit http://localhost:3000

    2. Drop 2-3 photos onto the drop zone. Wait for processing to complete (all show green "done").

    3. Verify SettingsPanel is visible below the drop zone in collapsed state (shows "▸ Settings").
       Click to expand — confirm relay list shows 3 relays (nostu.be, damus.io, nos.lol) and Blossom server shows 24242.io.

    4. Click "Upload N photos to Nostr" button.
       Confirm ProgressList shows "Encrypting" then "Uploading" for each photo (animated dots).

    5. After uploads: confirm "Publishing to Nostr..." spinner appears briefly.

    6. After publish: confirm optional album title field appears. Type a title, then check the share link appears in the code box.

    7. Click "Copy link" button. Paste into a new tab's address bar to confirm the link format is correct: should look like `/naddr1...#<base64url-key>`.

    8. Settings panel validation: expand settings, change Blossom server to "https://example.com" and click Save. Confirm error "Server does not allow browser uploads (CORS)" appears (example.com won't have proper CORS).

    9. Reload the page. Expand settings — confirm relay list and Blossom server values are still the defaults (custom example.com was rejected and not persisted).

    Report any issues found. The share link URL path will be tested in Phase 4.
  </how-to-verify>

  <resume-signal>Type "approved" to proceed, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
All unit tests GREEN (`npx vitest run`). TypeScript clean (`npx tsc --noEmit`). Human verified upload flow in browser. Share link generated after relay confirms.
</verification>

<success_criteria>
- ShareCard component shows spinner → title field → share link in correct sequence
- ProgressList shows encrypted status (purple) and uploading status (yellow) during upload
- UploadPanel wired with all Phase 3 hooks
- Upload button only appears after ALL photos are processing-done
- Share link only appears after relay responds OK (UPLD-08)
- Settings persist in localStorage (CONF-01, CONF-02)
- Human verification approved
</success_criteria>

<output>
After completion, create `.planning/phases/03-upload-and-publishing/03-06-SUMMARY.md`
</output>
