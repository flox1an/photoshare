---
phase: 03-upload-and-publishing
plan: 05
type: tdd
wave: 4
depends_on:
  - 03-04
files_modified:
  - src/hooks/useSettings.ts
  - src/components/upload/SettingsPanel.tsx
autonomous: true
requirements:
  - CONF-01
  - CONF-02

must_haves:
  truths:
    - "Settings panel is a collapsible section below the drop zone — no separate page"
    - "Relay list and Blossom server persist in localStorage and survive page reload"
    - "Blossom server is validated on save with HEAD request checking for CORS header"
    - "Invalid Blossom server shows 'Server does not allow browser uploads (CORS)' error inline"
    - "Default relay list is wss://relay.nostu.be, wss://relay.damus.io, wss://nos.lol"
    - "useSettings.test.ts tests pass GREEN"
  artifacts:
    - path: "src/hooks/useSettings.ts"
      provides: "Settings hook with localStorage persistence and Blossom validation"
      exports: ["useSettings"]
      contains: "localStorage"
    - path: "src/components/upload/SettingsPanel.tsx"
      provides: "Collapsible settings UI for relay list and Blossom server"
      contains: "collapsible\|isOpen\|open"
  key_links:
    - from: "src/hooks/useSettings.ts"
      to: "src/lib/blossom/validate.ts"
      via: "validateBlossomServer for CORS check on save"
      pattern: "validateBlossomServer"
    - from: "src/hooks/useSettings.ts"
      to: "src/lib/config.ts"
      via: "DEFAULT_RELAYS, DEFAULT_BLOSSOM_SERVER as fallbacks"
      pattern: "DEFAULT_RELAYS\|DEFAULT_BLOSSOM_SERVER"
    - from: "src/components/upload/SettingsPanel.tsx"
      to: "src/hooks/useSettings.ts"
      via: "useSettings() hook"
      pattern: "useSettings"
---

<objective>
Implement the settings hook with localStorage persistence and Blossom server CORS validation, then build the collapsible SettingsPanel UI component. Drive useSettings.test.ts from RED to GREEN.

Purpose: Users need to configure alternative relays and Blossom servers. Settings must survive page refresh and invalid servers must be rejected before any upload starts.
Output: useSettings.ts, SettingsPanel.tsx, useSettings.test.ts GREEN.
</objective>

<execution_context>
@/Users/flox/.claude/get-shit-done/workflows/execute-plan.md
@/Users/flox/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-upload-and-publishing/03-CONTEXT.md
@.planning/phases/03-upload-and-publishing/03-RESEARCH.md
@.planning/phases/03-upload-and-publishing/03-04-SUMMARY.md

<interfaces>
<!-- Key contracts for this plan. -->

From src/lib/config.ts (EXISTS):
```typescript
export const DEFAULT_RELAYS: string[]; // ["wss://relay.nostu.be"]
// CONTEXT.md locked decision overrides this for UI defaults:
// wss://relay.nostu.be, wss://relay.damus.io, wss://nos.lol
export const DEFAULT_BLOSSOM_SERVER: string; // "https://24242.io"
```

From src/lib/blossom/validate.ts (Plan 02 — exists):
```typescript
export async function validateBlossomServer(url: string): Promise<boolean>;
// Returns true only if HEAD request succeeds AND Access-Control-Allow-Origin header is present
```

From src/hooks/useUpload.ts (Plan 04 — exists):
```typescript
export interface UploadSettings {
  blossomServer: string;
  relays: string[];
}
// UploadPanel will call useSettings() and pass settings to useUpload's startUpload()
```

useSettings return type:
```typescript
interface UseSettingsReturn {
  relays: string[];
  setRelays: (relays: string[]) => void;
  blossomServer: string;
  setBlossomServer: (url: string) => Promise<void>; // async — validates before setting
  blossomError: string | null; // "Server does not allow browser uploads (CORS)" or null
  isValidating: boolean; // true during HEAD request
}
```

SettingsPanel props:
```typescript
interface SettingsPanelProps {
  settings: UseSettingsReturn;
}
```

localStorage keys:
- 'nostr-relays' → JSON.stringify(string[])
- 'blossom-server' → string (plain URL)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement useSettings hook with localStorage persistence and Blossom validation</name>
  <files>src/hooks/useSettings.ts</files>

  <read_first>
    - src/hooks/useSettings.test.ts — the RED tests to drive GREEN (already written in Plan 01)
    - src/lib/config.ts — DEFAULT_RELAYS, DEFAULT_BLOSSOM_SERVER values
    - src/lib/blossom/validate.ts — validateBlossomServer signature
    - .planning/phases/03-upload-and-publishing/03-RESEARCH.md — Pattern 7 (settings hook), Pitfall 5 (localStorage SSR safety)
    - .planning/phases/03-upload-and-publishing/03-CONTEXT.md — LOCKED: default relay list is 3 relays (nostu.be + damus.io + nos.lol)
  </read_first>

  <behavior>
    localStorage initialization (SSR safety):
    - Use lazy useState initializer: `useState<string[]>(() => { ... })` — runs client-side only
    - In lazy initializer: try localStorage.getItem('nostr-relays'), parse JSON, or fall back
    - SSR-safe pattern: 'use client' + lazy initializer handles SSR correctly (no need for dynamic import here)

    Default relay list:
    - CONTEXT.md LOCKED: ["wss://relay.nostu.be", "wss://relay.damus.io", "wss://nos.lol"]
    - This overrides DEFAULT_RELAYS from config.ts (which only has relay.nostu.be)
    - Define UI_DEFAULT_RELAYS = [...DEFAULT_RELAYS, "wss://relay.damus.io", "wss://nos.lol"] locally

    setBlossomServer (async):
    - Set isValidating: true
    - Call validateBlossomServer(url)
    - If false: set blossomError to "Server does not allow browser uploads (CORS)", do NOT update blossomServer state
    - If true: update blossomServer state, clear blossomError, persist to localStorage
    - Set isValidating: false in both paths

    Persistence (useEffect):
    - useEffect(() => { localStorage.setItem('nostr-relays', JSON.stringify(relays)) }, [relays])
    - useEffect(() => { localStorage.setItem('blossom-server', blossomServer) }, [blossomServer])
    - NOTE: blossomServer is only persisted AFTER successful validation (validation gates the state update)
  </behavior>

  <action>
    Create src/hooks/useSettings.ts:
    - 'use client' at top
    - Import DEFAULT_RELAYS, DEFAULT_BLOSSOM_SERVER from '@/lib/config'
    - Import validateBlossomServer from '@/lib/blossom/validate'
    - Define UI_DEFAULT_RELAYS = [...DEFAULT_RELAYS, "wss://relay.damus.io", "wss://nos.lol"]
    - Lazy useState for relays: reads 'nostr-relays' from localStorage
    - Lazy useState for blossomServer: reads 'blossom-server' from localStorage
    - useState for blossomError: string | null, starts null
    - useState for isValidating: boolean, starts false
    - setRelays: direct state setter + persists via useEffect
    - setBlossomServer: async function as described in behavior block
    - Export { useSettings } and UseSettingsReturn type

    After implementing:
    `npx vitest run src/hooks/useSettings.test.ts --reporter=verbose`
    jsdom provides localStorage — tests can call localStorage.clear() in beforeEach.
    If test uses renderHook, ensure @testing-library/react is available (installed in Phase 1 or 2).
  </action>

  <verify>
    <automated>npx vitest run src/hooks/useSettings.test.ts --reporter=verbose</automated>
  </verify>

  <acceptance_criteria>
    - npx vitest run src/hooks/useSettings.test.ts exits 0 (all tests GREEN)
    - grep "localStorage" src/hooks/useSettings.ts — exits 0
    - grep "validateBlossomServer" src/hooks/useSettings.ts — exits 0
    - grep "relay\.damus\.io\|nos\.lol" src/hooks/useSettings.ts — exits 0 (3 default relays)
    - grep "does not allow browser uploads\|CORS" src/hooks/useSettings.ts — exits 0 (error message)
    - grep "isValidating" src/hooks/useSettings.ts — exits 0
  </acceptance_criteria>

  <done>useSettings hook implemented; localStorage persistence works; Blossom validation gates state update; all useSettings.test.ts tests pass GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Build collapsible SettingsPanel component</name>
  <files>src/components/upload/SettingsPanel.tsx</files>

  <read_first>
    - src/components/upload/UploadPanel.tsx — layout structure and Tailwind patterns to match
    - src/components/upload/ProgressList.tsx — component style conventions
    - src/hooks/useSettings.ts — UseSettingsReturn type (just created)
    - .planning/phases/03-upload-and-publishing/03-CONTEXT.md — LOCKED: "Collapsible section below drop zone — always accessible, not a separate page"
  </read_first>

  <action>
    Create src/components/upload/SettingsPanel.tsx:
    - 'use client' at top
    - Import UseSettingsReturn from '@/hooks/useSettings'
    - Props: { settings: UseSettingsReturn }
    - Collapsible via useState(false) for isOpen
    - When closed: show a single "Settings" button/toggle with chevron icon (use text "▸ Settings" / "▾ Settings")
    - When open, show two sections:
      1. Relays section:
         - Label: "Nostr Relays"
         - Textarea (one URL per line) showing settings.relays.join('\n')
         - onChange: split by newline, filter empty, call settings.setRelays
         - Tailwind: `w-full rounded border border-gray-200 p-2 text-sm font-mono`
      2. Blossom Server section:
         - Label: "Blossom Server"
         - Input[type=text] showing settings.blossomServer
         - Button "Save" → calls settings.setBlossomServer(inputValue)
         - Show spinner (animate-spin) when settings.isValidating is true
         - Show settings.blossomError as red text below input if non-null
         - Tailwind: similar to relay textarea
    - Wrap the whole panel in a div with border, rounded corners, p-4, mt-4
    - Do NOT add a submit form — individual section saves are intentional
    - No animation on collapse/expand — just conditional render (no CSS transitions; user did not request animations)
  </action>

  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "SettingsPanel|error" || echo "TypeScript OK"</automated>
  </verify>

  <acceptance_criteria>
    - test -f src/components/upload/SettingsPanel.tsx — exits 0
    - grep "isOpen\|setIsOpen\|collapsible\|open" src/components/upload/SettingsPanel.tsx — exits 0 (collapsible present)
    - grep "isValidating" src/components/upload/SettingsPanel.tsx — exits 0 (spinner tied to validation)
    - grep "blossomError" src/components/upload/SettingsPanel.tsx — exits 0 (error display present)
    - grep "nostr-relays\|setRelays" src/components/upload/SettingsPanel.tsx — exits 0 (relay editing present)
    - npx tsc --noEmit exits 0
  </acceptance_criteria>

  <done>SettingsPanel component collapsible, shows relay list editor and Blossom server input with validation feedback, compiles TypeScript clean.</done>
</task>

</tasks>

<verification>
`npx vitest run src/hooks/useSettings.test.ts --reporter=verbose` exits 0. `npx tsc --noEmit` exits 0. SettingsPanel.tsx exists and TypeScript-compiles.
</verification>

<success_criteria>
- src/hooks/useSettings.ts exports useSettings with localStorage persistence
- Default relay list includes relay.nostu.be + relay.damus.io + nos.lol (CONF-01 locked decision)
- setBlossomServer validates CORS before persisting (CONF-02)
- src/components/upload/SettingsPanel.tsx is collapsible with relay + blossom sections
- All useSettings.test.ts tests GREEN
- TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/03-upload-and-publishing/03-05-SUMMARY.md`
</output>
