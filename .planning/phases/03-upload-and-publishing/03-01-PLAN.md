---
phase: 03-upload-and-publishing
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/blossom/upload.test.ts
  - src/lib/blossom/validate.test.ts
  - src/lib/nostr/event.test.ts
  - src/store/uploadStore.test.ts
  - src/hooks/useUpload.test.ts
  - src/hooks/useSettings.test.ts
autonomous: true
requirements:
  - UPLD-01
  - UPLD-02
  - UPLD-03
  - UPLD-04
  - UPLD-05
  - UPLD-06
  - UPLD-07
  - UPLD-08
  - CONF-01
  - CONF-02

must_haves:
  truths:
    - "All 6 test files exist and each test fails with 'not implemented' or import error (RED state)"
    - "Running `npx vitest run` reports all new tests as failing — no false greens"
    - "Test file imports reference the exact source module paths that implementation plans will create"
  artifacts:
    - path: "src/lib/blossom/upload.test.ts"
      provides: "RED tests for sha256Hex, buildBlossomUploadAuth, uploadBlob (UPLD-01, UPLD-02, UPLD-03)"
    - path: "src/lib/blossom/validate.test.ts"
      provides: "RED tests for validateBlossomServer CORS check (CONF-02)"
    - path: "src/lib/nostr/event.test.ts"
      provides: "RED tests for buildAlbumEvent kind/tags/expiry (UPLD-05, UPLD-06)"
    - path: "src/store/uploadStore.test.ts"
      provides: "RED tests for upload state transitions (UPLD-07)"
    - path: "src/hooks/useUpload.test.ts"
      provides: "RED tests for share link gate on relay OK (UPLD-08)"
    - path: "src/hooks/useSettings.test.ts"
      provides: "RED tests for localStorage relay/blossom persistence (CONF-01)"
  key_links:
    - from: "src/lib/blossom/upload.test.ts"
      to: "src/lib/blossom/upload.ts"
      via: "import { sha256Hex, buildBlossomUploadAuth, uploadBlob }"
      pattern: "from.*blossom/upload"
    - from: "src/lib/nostr/event.test.ts"
      to: "src/lib/nostr/event.ts"
      via: "import { buildAlbumEvent }"
      pattern: "from.*nostr/event"
---

<objective>
Create all Wave 0 test scaffolds in RED state. Every source module that Plan 02–05 will create gets a failing test file now, establishing the behavioral contracts before any implementation exists.

Purpose: Nyquist rule — every implementation task needs a pre-existing test. This plan creates the failing tests so Plans 02–05 can drive implementation to GREEN.
Output: 6 test files, all failing, covering all 10 Phase 3 requirements.
</objective>

<execution_context>
@/Users/flox/.claude/get-shit-done/workflows/execute-plan.md
@/Users/flox/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-upload-and-publishing/03-CONTEXT.md
@.planning/phases/03-upload-and-publishing/03-RESEARCH.md

<interfaces>
<!-- Key types and contracts test files must reference. Do NOT import from source — source doesn't exist yet. -->

From src/lib/crypto.ts (EXISTS):
```typescript
export async function encryptBlob(data: ArrayBuffer, key: CryptoKey): Promise<{ ciphertext: ArrayBuffer; iv: Uint8Array }>;
export async function generateAlbumKey(): Promise<CryptoKey>;
export function uint8ArrayToBase64url(bytes: Uint8Array): string;
```

From src/lib/config.ts (EXISTS):
```typescript
export const DEFAULT_RELAYS: string[]; // ["wss://relay.nostu.be"]
export const DEFAULT_BLOSSOM_SERVER: string; // "https://24242.io"
export const ALBUM_EXPIRY_SECONDS: number; // 2592000
export const BLOSSOM_EXPIRY_SECONDS: number; // 5184000
```

From src/types/album.ts (EXISTS):
```typescript
export interface PhotoEntry { hash, iv, thumbHash, thumbIv, width, height, filename }
export interface AlbumManifest { title?, createdAt, photos: PhotoEntry[] }
```

Source modules to import from (DON'T EXIST YET — tests must reference these paths so RED is from import failure):
  - @/lib/blossom/upload → { sha256Hex, buildBlossomUploadAuth, uploadBlob }
  - @/lib/blossom/validate → { validateBlossomServer }
  - @/lib/nostr/event → { buildAlbumEvent }
  - @/store/uploadStore → { useUploadStore }
  - @/hooks/useUpload → { useUpload }
  - @/hooks/useSettings → { useSettings }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write RED test scaffolds for Blossom upload library</name>
  <files>src/lib/blossom/upload.test.ts, src/lib/blossom/validate.test.ts</files>

  <read_first>
    - src/lib/crypto.ts — encryptBlob signature, return type
    - src/lib/config.ts — DEFAULT_BLOSSOM_SERVER, BLOSSOM_EXPIRY_SECONDS
    - .planning/phases/03-upload-and-publishing/03-RESEARCH.md — Pattern 1 (BUD-11 auth), Pattern 2 (SHA-256), Pattern 3 (uploadBlob), pitfall on base64 vs base64url
  </read_first>

  <behavior>
    upload.test.ts must cover:
    - sha256Hex(ArrayBuffer) → returns lowercase hex string of correct length (64 chars)
    - sha256Hex of known input matches expected SHA-256 hash
    - buildBlossomUploadAuth(signer, hashHex) → returns string starting with "Nostr " (UPLD-01)
    - Decoded Authorization header contains a JSON object with kind=24242, tags including ["t","upload"] and ["x", hashHex] (UPLD-01)
    - The kind 24242 event expiration tag is within [now+3500, now+3700] seconds (UPLD-03)
    - uploadBlob when server returns sha256 matching local hash → returns BlobDescriptor (UPLD-01, UPLD-02)
    - uploadBlob when server returns sha256 NOT matching local hash → throws error with "hash mismatch" in message (UPLD-02)
    - uploadBlob when server returns non-2xx → throws with status code in message

    validate.test.ts must cover:
    - validateBlossomServer(url) with fetch returning {ok:true, headers with access-control-allow-origin: "*"} → returns true (CONF-02)
    - validateBlossomServer(url) with fetch returning {ok:true} but no CORS header → returns false (CONF-02)
    - validateBlossomServer(url) when fetch throws (network error) → returns false (CONF-02)
  </behavior>

  <action>
    Create src/lib/blossom/upload.test.ts:
    - Import from "@/lib/blossom/upload" (does not exist — import will cause test to fail RED immediately)
    - Use `vi.stubGlobal('fetch', vi.fn())` for uploadBlob tests
    - For buildBlossomUploadAuth, create a mock PrivateKeySigner: `const signer = { signEvent: vi.fn(async (t) => ({ ...t, id: 'abc', sig: 'def', pubkey: 'pub' })) }`
    - Use vitest environment: // @vitest-environment jsdom

    Create src/lib/blossom/validate.test.ts:
    - Import from "@/lib/blossom/validate" (does not exist — RED)
    - Mock fetch with `vi.stubGlobal('fetch', vi.fn())`
    - Test the three cases from behavior block above
    - Use beforeEach to restore fetch mock: `vi.restoreAllMocks()`

    Both files: do NOT create the source modules. Tests must fail because the imported module does not exist.
    After writing, run `npx vitest run src/lib/blossom/ --reporter=verbose` and confirm all tests FAIL.
  </action>

  <verify>
    <automated>npx vitest run src/lib/blossom/ --reporter=verbose 2>&1 | grep -E "FAIL|Cannot find module|failed"</automated>
  </verify>

  <acceptance_criteria>
    - grep -r "from.*@/lib/blossom/upload" src/lib/blossom/upload.test.ts — exits 0
    - grep -r "from.*@/lib/blossom/validate" src/lib/blossom/validate.test.ts — exits 0
    - grep "hash mismatch" src/lib/blossom/upload.test.ts — exits 0
    - grep "access-control-allow-origin" src/lib/blossom/validate.test.ts — exits 0 (case-insensitive)
    - npx vitest run src/lib/blossom/ exits non-zero (tests fail RED)
  </acceptance_criteria>

  <done>Both test files exist, import from non-existent source paths, and all tests fail.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write RED test scaffolds for Nostr event builder, upload store, useUpload, useSettings</name>
  <files>src/lib/nostr/event.test.ts, src/store/uploadStore.test.ts, src/hooks/useUpload.test.ts, src/hooks/useSettings.test.ts</files>

  <read_first>
    - src/types/album.ts — AlbumManifest, PhotoEntry interfaces
    - src/lib/config.ts — ALBUM_EXPIRY_SECONDS, DEFAULT_RELAYS, DEFAULT_BLOSSOM_SERVER
    - src/store/processingStore.ts — Zustand create pattern to mirror
    - .planning/phases/03-upload-and-publishing/03-RESEARCH.md — Pattern 4 (buildAlbumEvent), Pattern 5 (RelayPool.publish), Pattern 6 (upload store states)
  </read_first>

  <behavior>
    event.test.ts must cover:
    - buildAlbumEvent(signer, encryptedManifestB64url, manifestIvB64url, dTag) returns event with kind=30078 (UPLD-05)
    - Returned event has tag ["d", dTag] (UPLD-05)
    - Returned event has tag ["iv", manifestIvB64url] (UPLD-05)
    - Returned event has tag ["alt", "Encrypted photo album"] (UPLD-05)
    - Returned event has expiration tag within [now + 2592000 - 60, now + 2592000 + 60] seconds (UPLD-06)
    - Returned event content equals encryptedManifestB64url (UPLD-05)

    uploadStore.test.ts must cover:
    - Initial state: photos is empty Record (UPLD-07)
    - setEncrypting(id) transitions photo from pending to 'encrypting' (UPLD-07)
    - setUploading(id) transitions photo from encrypting to 'uploading' (UPLD-07)
    - setUploadDone(id, descriptor) transitions to 'done' with BlobDescriptor result (UPLD-07)
    - setUploadError(id, message) transitions to 'error' with message (UPLD-07)
    - Status union includes 'encrypting' and 'uploading' in addition to existing values

    useUpload.test.ts must cover:
    - startUpload() with mocked RelayPool returning [{ ok: false }] → shareLink remains null (UPLD-08)
    - startUpload() with mocked RelayPool returning [{ ok: true }] AND all blobs uploaded → shareLink is non-null string (UPLD-08)

    useSettings.test.ts must cover:
    - Initial relays value falls back to DEFAULT_RELAYS when localStorage is empty (CONF-01)
    - After setRelays(['wss://custom.relay']), localStorage.getItem('nostr-relays') contains the value (CONF-01)
    - Initial blossomServer falls back to DEFAULT_BLOSSOM_SERVER when localStorage is empty (CONF-02)
    - After setBlossomServer('https://other.server'), localStorage.getItem('blossom-server') equals 'https://other.server' (CONF-02)
  </behavior>

  <action>
    Create src/lib/nostr/event.test.ts:
    - // @vitest-environment jsdom at top
    - Import { buildAlbumEvent } from "@/lib/nostr/event" (does not exist — RED)
    - Mock signer: `{ signEvent: vi.fn(async (t) => ({ ...t, id: 'fakeid', sig: 'fakesig', pubkey: 'fakepub' })) }`
    - For expiration tag: find tag with tag[0] === 'expiration', parse tag[1] as number, check range

    Create src/store/uploadStore.test.ts:
    - // @vitest-environment jsdom
    - Import { useUploadStore } from "@/store/uploadStore" (does not exist — RED)
    - Test state transitions using `useUploadStore.getState()` (same pattern as processingStore)
    - Run store.reset() in beforeEach

    Create src/hooks/useUpload.test.ts:
    - // @vitest-environment jsdom
    - Import { useUpload } from "@/hooks/useUpload" (does not exist — RED)
    - Mock RelayPool: `vi.mock('applesauce-relay/pool', () => ({ RelayPool: vi.fn() }))`
    - Keep tests minimal — just the two share link gate cases from behavior block
    - Note: useUpload is a React hook; wrap in renderHook from @testing-library/react if needed

    Create src/hooks/useSettings.test.ts:
    - // @vitest-environment jsdom
    - Import { useSettings } from "@/hooks/useSettings" (does not exist — RED)
    - jsdom provides localStorage — use it directly in test assertions
    - Use beforeEach(() => localStorage.clear()) to reset state between tests
    - Wrap hook in renderHook from @testing-library/react

    After writing all 4 files, run: `npx vitest run src/lib/nostr/event.test.ts src/store/uploadStore.test.ts src/hooks/useUpload.test.ts src/hooks/useSettings.test.ts --reporter=verbose`
    Confirm all FAIL (import errors = RED is established).
  </action>

  <verify>
    <automated>npx vitest run src/lib/nostr/event.test.ts src/store/uploadStore.test.ts src/hooks/useUpload.test.ts src/hooks/useSettings.test.ts --reporter=verbose 2>&1 | grep -E "FAIL|Cannot find module|failed"</automated>
  </verify>

  <acceptance_criteria>
    - grep "from.*@/lib/nostr/event" src/lib/nostr/event.test.ts — exits 0
    - grep "from.*@/store/uploadStore" src/store/uploadStore.test.ts — exits 0
    - grep "from.*@/hooks/useUpload" src/hooks/useUpload.test.ts — exits 0
    - grep "from.*@/hooks/useSettings" src/hooks/useSettings.test.ts — exits 0
    - grep "expiration" src/lib/nostr/event.test.ts — exits 0
    - grep "encrypting" src/store/uploadStore.test.ts — exits 0
    - grep "shareLink" src/hooks/useUpload.test.ts — exits 0
    - grep "DEFAULT_RELAYS\|nostr-relays" src/hooks/useSettings.test.ts — exits 0
    - npx vitest run (above 4 files) exits non-zero (all RED)
  </acceptance_criteria>

  <done>All 4 test files exist, import from non-existent source paths, and all tests fail in RED state.</done>
</task>

</tasks>

<verification>
All 6 test files exist, import from non-existent source modules, and `npx vitest run` reports failures for all of them. No source implementation files exist yet. Zero false-green tests.
</verification>

<success_criteria>
- 6 test files created under correct paths
- All test files import from modules that do not yet exist (RED by import failure)
- `npx vitest run src/lib/blossom/ src/lib/nostr/event.test.ts src/store/uploadStore.test.ts src/hooks/` exits non-zero
- Each test file covers the behaviors listed in its behavior block
- No source implementation files created in this plan
</success_criteria>

<output>
After completion, create `.planning/phases/03-upload-and-publishing/03-01-SUMMARY.md`
</output>
