---
phase: 03-upload-and-publishing
plan: 03
type: tdd
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/store/uploadStore.ts
  - src/types/processing.ts
autonomous: true
requirements:
  - UPLD-07

must_haves:
  truths:
    - "Upload status union includes 'encrypting' and 'uploading' in addition to processing states"
    - "uploadStore exposes setEncrypting, setUploading, setUploadDone, setUploadError actions"
    - "ProgressList can show encrypting and uploading status dots with distinct visual treatment"
    - "All uploadStore.test.ts tests pass GREEN"
  artifacts:
    - path: "src/store/uploadStore.ts"
      provides: "Zustand upload state store with 6-state status union"
      exports: ["useUploadStore"]
      contains: "encrypting"
    - path: "src/types/processing.ts"
      provides: "Extended PhotoProcessingStatus union with upload states"
      contains: "encrypting.*uploading|uploading.*encrypting"
  key_links:
    - from: "src/store/uploadStore.ts"
      to: "src/types/processing.ts"
      via: "imports PhotoUploadStatus union"
      pattern: "from.*types/processing"
    - from: "src/components/upload/ProgressList.tsx"
      to: "src/store/uploadStore.ts"
      via: "StatusDot must handle new status values (wired in Plan 06)"
      pattern: "useUploadStore"
---

<objective>
Implement the upload Zustand store with a 6-state status progression (pending → processing → encrypting → uploading → done | error) and update the processing types to include upload states. Drive uploadStore.test.ts from RED to GREEN.

Purpose: The unified status union means ProgressList shows one coherent status per photo across both Phase 2 processing and Phase 3 upload — no dual-store complexity.
Output: uploadStore.ts, updated processing.ts type, uploadStore.test.ts GREEN.
</objective>

<execution_context>
@/Users/flox/.claude/get-shit-done/workflows/execute-plan.md
@/Users/flox/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-upload-and-publishing/03-CONTEXT.md
@.planning/phases/03-upload-and-publishing/03-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts for this plan. -->

From src/types/processing.ts (EXISTS — will be modified):
```typescript
// Current:
export type PhotoProcessingStatus = 'pending' | 'processing' | 'done' | 'error';

// After this plan (add upload states):
export type PhotoProcessingStatus = 'pending' | 'processing' | 'encrypting' | 'uploading' | 'done' | 'error';
```

From src/store/processingStore.ts (EXISTS — pattern to follow):
```typescript
import { create } from 'zustand';
// create<State>((set) => ({ ... })) pattern
// useProcessingStore.getState() in tests
```

Target upload store shape:
```typescript
interface UploadStoreState {
  photos: Record<string, PhotoUploadState>;
  // Actions:
  setEncrypting(id: string): void;
  setUploading(id: string): void;
  setUploadDone(id: string, descriptor: { hash: string; thumbHash: string }): void;
  setUploadError(id: string, error: string): void;
  reset(): void;
}
```

Test file (EXISTS from Plan 01):
- src/store/uploadStore.test.ts — imports from "@/store/uploadStore"
- Tests call setEncrypting, setUploading, setUploadDone, setUploadError
- Tests use useUploadStore.getState() to read state
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend processing types and implement upload Zustand store</name>
  <files>src/types/processing.ts, src/store/uploadStore.ts</files>

  <read_first>
    - src/store/uploadStore.test.ts — the RED tests to drive GREEN (already written in Plan 01)
    - src/store/processingStore.ts — exact Zustand create pattern to mirror
    - src/types/processing.ts — current PhotoProcessingStatus union to extend
    - src/types/blossom.ts — BlobDescriptor (created in Plan 02 or will be available)
    - .planning/phases/03-upload-and-publishing/03-RESEARCH.md — Pattern 6 (upload store states), UPLD-07 description
  </read_first>

  <behavior>
    types/processing.ts change:
    - Extend PhotoProcessingStatus to add 'encrypting' | 'uploading' to the union
    - This is a backward-compatible addition — existing 'pending'|'processing'|'done'|'error' remain
    - ProgressList.tsx currently uses `colors: Record<typeof status, string>` — the StatusDot colors
      map must be updated in Plan 06 when ProgressList is wired; this plan does NOT touch ProgressList

    uploadStore.ts:
    - Define PhotoUploadState interface: { id: string; filename: string; status: PhotoProcessingStatus; hash?: string; thumbHash?: string; error?: string }
    - Create useUploadStore with actions:
      - setEncrypting(id): status → 'encrypting'
      - setUploading(id): status → 'uploading'
      - setUploadDone(id, { hash, thumbHash }): status → 'done', sets hash and thumbHash
      - setUploadError(id, error): status → 'error', sets error message
      - reset(): wipes photos back to {}
    - NOTE: The upload store does NOT have an addPhotos action — photos are added to the processing
      store in Phase 2 and the upload hook reads ProcessedPhoto results from there. The upload store
      only tracks upload-phase transitions for photos that have already been processed.
    - Follow same Zustand create<State>((set) => ({...})) pattern as processingStore.ts exactly

    After implementing, run the test file. Expect GREEN on all store transition tests.
  </behavior>

  <action>
    1. Modify src/types/processing.ts:
       - Change PhotoProcessingStatus to: `'pending' | 'processing' | 'encrypting' | 'uploading' | 'done' | 'error'`
       - No other changes — existing PhotoProcessingState, ProcessedPhoto, ProcessorApi unchanged
       - IMPORTANT: This is the canonical status type shared by both processing and upload stores

    2. Create src/store/uploadStore.ts:
       - Import `create` from 'zustand'
       - Import `PhotoProcessingStatus` from '@/types/processing'
       - Define PhotoUploadState interface (id, filename, status, hash?, thumbHash?, error?)
       - Create UploadStoreState interface with photos Record and the 4 action methods + reset
       - Implement with create<UploadStoreState>((set) => ({...})) exactly like processingStore.ts
       - Export `useUploadStore`

    3. Run: `npx vitest run src/store/uploadStore.test.ts --reporter=verbose`
       All tests must pass GREEN. Also run `npx vitest run src/store/ --reporter=verbose` to confirm
       processingStore tests still pass (the type change must be backward compatible).
  </action>

  <verify>
    <automated>npx vitest run src/store/ --reporter=verbose</automated>
  </verify>

  <acceptance_criteria>
    - npx vitest run src/store/ exits 0 (all store tests GREEN, including processingStore)
    - grep "encrypting.*uploading\|uploading.*encrypting" src/types/processing.ts — exits 0
    - grep "useUploadStore" src/store/uploadStore.ts — exits 0
    - grep "setEncrypting\|setUploading\|setUploadDone\|setUploadError" src/store/uploadStore.ts — exits 0 (all 4 actions exist)
    - grep "reset" src/store/uploadStore.ts — exits 0
    - npx tsc --noEmit exits 0 (no TypeScript errors from extending the union)
  </acceptance_criteria>

  <done>PhotoProcessingStatus extended with upload states; useUploadStore implemented; all store tests pass GREEN; TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
`npx vitest run src/store/ --reporter=verbose` exits 0. `npx tsc --noEmit` has no type errors. Processing store tests still GREEN (backward-compatible type extension).
</verification>

<success_criteria>
- src/types/processing.ts PhotoProcessingStatus includes 'encrypting' and 'uploading'
- src/store/uploadStore.ts exports useUploadStore with 4 transition actions + reset
- All tests in src/store/ pass GREEN
- processingStore tests still pass (type extension is backward compatible)
</success_criteria>

<output>
After completion, create `.planning/phases/03-upload-and-publishing/03-03-SUMMARY.md`
</output>
