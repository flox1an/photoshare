---
phase: 04-share-link-and-viewer
plan: "05"
type: execute
wave: 5
depends_on:
  - "04-03"
  - "04-04"
files_modified:
  - src/components/viewer/DownloadProgress.tsx
  - src/components/viewer/ViewerPanel.tsx
autonomous: false
requirements:
  - VIEW-01
  - VIEW-02
  - VIEW-03
  - VIEW-04
  - VIEW-05
  - VIEW-06
  - VIEW-07
  - CONF-03

must_haves:
  truths:
    - "Opening a share link in a fresh browser tab shows the thumbnail grid within seconds"
    - "Clicking a thumbnail opens the lightbox at the correct position"
    - "Arrow keys and touch swipe navigate the lightbox"
    - "Pressing Escape or clicking the overlay background closes the lightbox"
    - "Full-resolution image appears when the lightbox opens (blurred thumb shows immediately)"
    - "Download all button triggers ZIP file download with correct filenames"
    - "Progress bar shows 'Downloading X/Y...' while fetching and decrypting"
    - "Error state shows meaningful message for invalid/expired links"
    - "Gallery is usable on mobile portrait with the lightbox fitting the screen"
  artifacts:
    - path: "src/components/viewer/DownloadProgress.tsx"
      provides: "Download progress bar component"
      exports: ["default DownloadProgress"]
    - path: "src/components/viewer/ViewerPanel.tsx"
      provides: "Fully wired viewer: grid + lightbox + download + error states"
      exports: ["default ViewerPanel"]
  key_links:
    - from: "src/components/viewer/ViewerPanel.tsx"
      to: "src/hooks/useAlbumViewer.ts"
      via: "const viewer = useAlbumViewer({ naddr })"
      pattern: "useAlbumViewer"
    - from: "src/components/viewer/ViewerPanel.tsx"
      to: "src/components/viewer/ThumbnailGrid.tsx"
      via: "photos={manifest.photos} objectUrls={viewer.thumbUrls}"
      pattern: "ThumbnailGrid"
    - from: "src/components/viewer/ViewerPanel.tsx"
      to: "src/components/viewer/Lightbox.tsx"
      via: "currentIndex={lightboxIndex} onClose={() => setLightboxIndex(null)}"
      pattern: "Lightbox"
---

<objective>
Wire the complete viewer: implement `DownloadProgress` component, then replace the ViewerPanel stub with the fully assembled viewer that connects `useAlbumViewer` to `ThumbnailGrid`, `Lightbox`, and `DownloadProgress`. Human verification confirms the end-to-end flow in a real browser.

Purpose: Ship the viewer. Everything built in Plans 01-04 comes together here. After human verification, Phase 4 is complete.
Output: Working viewer at `/view/[naddr]#<key>` — thumbnail grid, lightbox, download-all.
</objective>

<execution_context>
@/Users/flox/.claude/get-shit-done/workflows/execute-plan.md
@/Users/flox/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-share-link-and-viewer/04-CONTEXT.md
@.planning/phases/04-share-link-and-viewer/04-RESEARCH.md
@.planning/phases/04-share-link-and-viewer/04-03-SUMMARY.md
@.planning/phases/04-share-link-and-viewer/04-04-SUMMARY.md

@src/components/viewer/ViewerPanel.tsx
@src/hooks/useAlbumViewer.ts
@src/components/viewer/ThumbnailGrid.tsx
@src/components/viewer/Lightbox.tsx
@src/types/album.ts

<interfaces>
<!-- What this plan wires together. From previous plans. -->

useAlbumViewer({ naddr }) returns:
```typescript
{
  status: 'loading' | 'ready' | 'error';
  error: string | null;
  manifest: AlbumManifest | null;
  thumbUrls: Record<string, string>;      // key = photo.thumbHash
  fullUrls: Record<string, string>;       // key = photo.hash
  albumKey: CryptoKey | null;
  blossomServer: string;
  downloadAll(): Promise<void>;
  downloadProgress: { current: number; total: number } | null;
  loadThumbnail(index: number): void;
  loadFullImage(index: number): void;
}
```

ThumbnailGrid props:
```typescript
{ photos, objectUrls, loadThumbnail, onPhotoClick }
```

Lightbox props:
```typescript
{ photos, currentIndex, thumbUrls, fullUrls, albumKey, blossomServer, onNext, onPrev, onClose }
```

DownloadProgress (new):
```typescript
interface DownloadProgressProps {
  current: number;
  total: number;
}
```

ViewerPanel already has correct SSR boundary in page.tsx:
- dynamic(..., { ssr: false }) wraps ViewerPanel
- React.use(params) extracts naddr in the page
- ViewerPanel receives naddr: string prop
- window.location.hash is safe inside ViewerPanel's useEffect (via useAlbumViewer)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement DownloadProgress and wire ViewerPanel</name>
  <files>src/components/viewer/DownloadProgress.tsx, src/components/viewer/ViewerPanel.tsx</files>
  <read_first>
    - src/components/viewer/ViewerPanel.tsx (existing stub to replace)
    - src/hooks/useAlbumViewer.ts (hook return shape — read the actual file)
    - src/components/viewer/ThumbnailGrid.tsx (props interface)
    - src/components/viewer/Lightbox.tsx (props interface)
    - src/types/album.ts (AlbumManifest.title, AlbumManifest.photos)
    - .planning/phases/04-share-link-and-viewer/04-CONTEXT.md (gallery header design: title + count + Download all button)
  </read_first>
  <action>
    Create src/components/viewer/DownloadProgress.tsx:
    - "use client" directive
    - Props: { current: number; total: number }
    - Renders: a progress bar + "Downloading {current}/{total}..." text
    - Progress bar: <div className="h-2 bg-blue-500 rounded transition-all" style={{ width: `${(current / total) * 100}%` }} /> inside a gray container

    Replace src/components/viewer/ViewerPanel.tsx entirely:
    - Keep "use client" directive at top
    - Props: { naddr: string }
    - Call const viewer = useAlbumViewer({ naddr })

    Loading state: when viewer.status === 'loading', show full-screen centered spinner or "Loading album..."

    Error state: when viewer.status === 'error', show centered error card with viewer.error message and a "This link may be invalid or the album may have expired." sub-message

    Ready state (viewer.status === 'ready', viewer.manifest is not null):
    - Header: flex row, album title (viewer.manifest.title ?? 'Photo Album'), photo count, and "Download all" button
    - Download all button: onClick={handleDownloadAll}, disabled while viewer.downloadProgress !== null
    - If viewer.downloadProgress !== null: render <DownloadProgress current={viewer.downloadProgress.current} total={viewer.downloadProgress.total} />
    - ThumbnailGrid: photos={viewer.manifest.photos} objectUrls={viewer.thumbUrls} loadThumbnail={viewer.loadThumbnail} onPhotoClick={handleOpenLightbox}
    - Lightbox (when lightboxIndex !== null):
      - currentIndex={lightboxIndex}
      - thumbUrls={viewer.thumbUrls}
      - fullUrls={viewer.fullUrls}
      - albumKey={viewer.albumKey}
      - blossomServer={viewer.blossomServer}
      - onNext={() => setLightboxIndex(i => Math.min(i! + 1, viewer.manifest!.photos.length - 1))}
      - onPrev={() => setLightboxIndex(i => Math.max(i! - 1, 0))}
      - onClose={() => setLightboxIndex(null)}

    Local state: const [lightboxIndex, setLightboxIndex] = useState<number | null>(null)

    handleOpenLightbox(index: number): setLightboxIndex(index), then call viewer.loadFullImage(index) for preloading

    handleDownloadAll: async wrapper that calls viewer.downloadAll() and catches errors to show an alert

    Run full suite: npx vitest run
    Run build: npm run build
    Commit: git commit -m "feat(04-05): wire ViewerPanel with all viewer components (VIEW-01 through VIEW-07, CONF-03)"
  </action>
  <verify>
    <automated>npx vitest run && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - npx vitest run exits 0 (all tests including Wave 0 files GREEN)
    - npm run build exits 0 (no TypeScript errors)
    - grep "useAlbumViewer" src/components/viewer/ViewerPanel.tsx exits 0
    - grep "ThumbnailGrid" src/components/viewer/ViewerPanel.tsx exits 0
    - grep "Lightbox" src/components/viewer/ViewerPanel.tsx exits 0
    - grep "DownloadProgress" src/components/viewer/ViewerPanel.tsx exits 0
    - The viewer stub no longer contains "PhotoShare Viewer" placeholder text
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify — end-to-end viewer in browser</name>
  <files>none</files>
  <what-built>
    Complete album viewer: thumbnail grid with shimmer loading, full-screen lightbox with keyboard/swipe navigation, blurred-thumbnail-to-full-res crossfade, and download-all ZIP. Built against the real share link format from Phase 3.
  </what-built>
  <how-to-verify>
    **Pre-requisite:** You need a real share link from Phase 3. Run the dev server:
    ```
    npm run dev
    ```
    Then upload photos at http://localhost:3000 to generate a share link.

    **Test 1 — Thumbnail grid (VIEW-01, VIEW-03):**
    1. Open the share link in a NEW browser tab (no prior state)
    2. Confirm: thumbnail grid appears, shimmer placeholders show while loading
    3. Confirm: thumbnails appear with correct aspect ratios (not square-cropped)
    4. Confirm: photo count is shown in the header (e.g., "12 photos")

    **Test 2 — Lightbox (VIEW-04):**
    5. Click a thumbnail — lightbox should open
    6. Confirm: blurred thumbnail visible immediately; full-res loads and fades in
    7. Confirm: photo counter shows "1 / N" at bottom
    8. Press ArrowRight / ArrowLeft — confirm navigation
    9. Press Escape — confirm lightbox closes
    10. Click the dark overlay background — confirm lightbox closes

    **Test 3 — Mobile responsive (VIEW-07):**
    11. Open Chrome DevTools, toggle mobile device simulation (iPhone SE)
    12. Confirm: 3-column grid is legible
    13. Confirm: lightbox fills the screen, image is not clipped
    14. Swipe left/right on the lightbox image — confirm navigation

    **Test 4 — Download all (VIEW-05):**
    15. Click "Download all" button — progress bar should appear
    16. Confirm: "Downloading X/N..." updates as photos are fetched
    17. Confirm: a photos.zip file is downloaded when complete
    18. Open the ZIP — confirm original filenames (e.g., IMG_2847.jpg), not hash-based names

    **Test 5 — Error state:**
    19. Open http://localhost:3000/view/naddr1invalidlink — confirm error message appears (not a crash)

    **Test 6 — Lazy loading (VIEW-06):**
    20. Upload a 10+ photo album, open the viewer, scroll slowly — confirm thumbnails load as they enter the viewport (not all at once on page load)
  </how-to-verify>
  <action>User performs browser verification steps above. Claude waits for approval signal.</action>
  <verify>
    <automated>echo "Manual verification required — see how-to-verify instructions above"</automated>
  </verify>
  <done>User types "approved" after all 6 test groups pass in the browser.</done>
  <resume-signal>Type "approved" if all tests pass, or describe any issues found so they can be fixed before approval.</resume-signal>
</task>

</tasks>

<verification>
Automated:
- npx vitest run — all tests GREEN (including all 5 Wave 0 test files)
- npm run build — exits 0, no TypeScript errors

Manual (checkpoint):
- Thumbnail grid renders in fresh tab
- Lightbox opens and navigates
- Mobile viewport usable
- Download all produces correct ZIP
- Error state handles invalid links gracefully
</verification>

<success_criteria>
ViewerPanel fully wired. All 8 phase requirements covered:
- VIEW-01: manifest decrypted from URL key + Nostr event
- VIEW-02: blobs fetched from Blossom by hash
- VIEW-03: thumbnail grid with skeletons
- VIEW-04: lightbox with keyboard + swipe
- VIEW-05: download-all with JSZip + progress bar
- VIEW-06: IntersectionObserver lazy loading
- VIEW-07: mobile responsive + touch swipe
- CONF-03: relay hints encoded in naddr (handled by loadAlbumEvent followRelayHints: true)
Human verification checkpoint APPROVED.
</success_criteria>

<output>
After completion, create `.planning/phases/04-share-link-and-viewer/04-05-SUMMARY.md`
</output>
