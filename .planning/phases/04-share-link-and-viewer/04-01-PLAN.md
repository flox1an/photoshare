---
phase: 04-share-link-and-viewer
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/nostr/viewer.test.ts
  - src/lib/blossom/fetch.test.ts
  - src/components/viewer/ThumbnailGrid.test.tsx
  - src/components/viewer/Lightbox.test.tsx
  - src/hooks/useAlbumViewer.test.ts
autonomous: true
requirements:
  - VIEW-01
  - VIEW-02
  - VIEW-03
  - VIEW-04
  - VIEW-05
  - VIEW-06
  - VIEW-07
  - CONF-03

must_haves:
  truths:
    - "5 test files exist with failing tests covering all viewer requirements"
    - "npx vitest run src/lib/nostr/viewer.test.ts fails (no implementation yet)"
    - "npx vitest run src/lib/blossom/fetch.test.ts fails (no implementation yet)"
    - "npx vitest run src/components/viewer/ThumbnailGrid.test.tsx fails"
    - "npx vitest run src/components/viewer/Lightbox.test.tsx fails"
    - "npx vitest run src/hooks/useAlbumViewer.test.ts fails"
  artifacts:
    - path: "src/lib/nostr/viewer.test.ts"
      provides: "Tests for decryptManifest and relay hint loading (VIEW-01, CONF-03)"
    - path: "src/lib/blossom/fetch.test.ts"
      provides: "Tests for fetchBlob BUD-01 GET endpoint (VIEW-02)"
    - path: "src/components/viewer/ThumbnailGrid.test.tsx"
      provides: "Tests for grid render + IntersectionObserver lazy load (VIEW-03, VIEW-06)"
    - path: "src/components/viewer/Lightbox.test.tsx"
      provides: "Tests for prev/next/close state transitions + keyboard (VIEW-04, VIEW-07)"
    - path: "src/hooks/useAlbumViewer.test.ts"
      provides: "Tests for download all + JSZip integration (VIEW-05)"
  key_links:
    - from: "src/lib/nostr/viewer.test.ts"
      to: "src/lib/nostr/viewer.ts"
      via: "import { decryptManifest, loadAlbumEvent }"
      pattern: "decryptManifest|loadAlbumEvent"
    - from: "src/lib/blossom/fetch.test.ts"
      to: "src/lib/blossom/fetch.ts"
      via: "import { fetchBlob }"
      pattern: "fetchBlob"
---

<objective>
Wave 0: Create 5 failing test files covering all Phase 4 viewer requirements. Tests are written first (RED state) to define the contracts that implementation plans must satisfy. This is the TDD gate — no implementation ships without a failing test first.

Purpose: Define exact API contracts for viewer logic libraries, hook, and UI components before any implementation. Tests are the specification.
Output: 5 test files, all failing, locked to specific import paths that implementation plans will create.
</objective>

<execution_context>
@/Users/flox/.claude/get-shit-done/workflows/execute-plan.md
@/Users/flox/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-share-link-and-viewer/04-CONTEXT.md
@.planning/phases/04-share-link-and-viewer/04-RESEARCH.md
@.planning/phases/04-share-link-and-viewer/04-VALIDATION.md

@src/types/album.ts
@src/lib/crypto.ts
@src/lib/nostr/naddr.ts
@vitest.config.ts
@vitest.setup.ts

<interfaces>
<!-- Existing types the tests will reference. Executor must use these exactly. -->

From src/types/album.ts:
```typescript
export interface PhotoEntry {
  hash: string;       // SHA-256 hex of encrypted full-size blob
  iv: string;         // base64url 12-byte IV for full-size
  thumbHash: string;  // SHA-256 hex of encrypted thumbnail blob
  thumbIv: string;    // base64url 12-byte IV for thumbnail
  width: number;      // original image width
  height: number;     // original image height
  filename: string;   // e.g. "IMG_2847.jpg"
}

export interface AlbumManifest {
  title?: string;
  createdAt: string;
  photos: PhotoEntry[];
}
```

From src/lib/crypto.ts:
```typescript
export async function decryptBlob(ciphertext: ArrayBuffer, key: CryptoKey, iv: Uint8Array): Promise<ArrayBuffer>
export async function importKeyFromBase64url(b64url: string): Promise<CryptoKey>
export function base64urlToUint8Array(b64url: string): Uint8Array
```

From src/lib/nostr/naddr.ts:
```typescript
export function decodeAlbumNaddr(naddr: string): nip19.AddressPointer
// Returns: { kind: 30078, pubkey, identifier, relays: string[] }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create viewer.test.ts and fetch.test.ts (logic layer contracts)</name>
  <files>src/lib/nostr/viewer.test.ts, src/lib/blossom/fetch.test.ts</files>
  <read_first>
    - src/lib/crypto.ts (decryptBlob, importKeyFromBase64url, base64urlToUint8Array signatures)
    - src/lib/nostr/naddr.ts (decodeAlbumNaddr return type)
    - src/types/album.ts (AlbumManifest, PhotoEntry interfaces)
    - vitest.config.ts (test environment settings)
    - vitest.setup.ts (Uint8Array patch — already in place)
  </read_first>
  <behavior>
    viewer.test.ts must cover:
    - VIEW-01 + CONF-03: "decryptManifest returns AlbumManifest given encrypted event content, IV tag, and CryptoKey" — mocks createAddressLoader to emit a fake event; asserts manifest.photos has correct length
    - VIEW-01: "loadAlbumEvent calls createAddressLoader with followRelayHints: true" — spy on createAddressLoader call options
    - VIEW-01: "loadAlbumEvent throws on EmptyError (expired album)" — mock observable that never emits; assert catch "Album not found"

    fetch.test.ts must cover:
    - VIEW-02: "fetchBlob returns ArrayBuffer on 200 response" — vi.stubGlobal('fetch', ...) returning ok: true + arrayBuffer()
    - VIEW-02: "fetchBlob throws on non-200 response" — vi.stubGlobal('fetch', ...) returning ok: false, status: 404
    - VIEW-02: "fetchBlob constructs correct BUD-01 URL (server + sha256, no trailing slash)" — assert fetch called with 'https://example.com/abc123'
  </behavior>
  <action>
    Create src/lib/nostr/viewer.test.ts importing from '@/lib/nostr/viewer' (file does not exist yet — this makes tests RED).
    Import { decryptManifest, loadAlbumEvent } from '@/lib/nostr/viewer'.
    Import { vi, describe, it, expect, beforeEach } from 'vitest'.
    Mock 'applesauce-loaders/loaders/address-loader' with vi.mock so createAddressLoader returns a function that produces an rxjs Observable.
    Mock 'applesauce-relay/pool' with vi.mock so RelayPool is a no-op constructor.
    For the decryptManifest test: use real Web Crypto API (available in vitest jsdom). Generate a test key with generateAlbumKey(), encrypt a test manifest string with encryptBlob(), then assert decryptManifest() returns the parsed object.
    For the followRelayHints test: spy on the createAddressLoader mock and assert it was called with { followRelayHints: true }.
    For the EmptyError test: mock the observable with EMPTY from rxjs and assert the loadAlbumEvent promise rejects with a message matching /not found|expired/i.

    Create src/lib/blossom/fetch.test.ts importing from '@/lib/blossom/fetch' (does not exist yet — RED).
    Import { fetchBlob } from '@/lib/blossom/fetch'.
    Use vi.stubGlobal('fetch', ...) in beforeEach; restore with vi.unstubAllGlobals() in afterEach.
    Test URL construction: server 'https://example.com/' (trailing slash) + hash 'abc123' should produce fetch call to 'https://example.com/abc123'.
    Test happy path: mock fetch returning Response with ok: true and arrayBuffer() returning an ArrayBuffer.
    Test error path: mock fetch returning Response with ok: false, status: 404.
    Run tests to confirm RED: npx vitest run src/lib/nostr/viewer.test.ts src/lib/blossom/fetch.test.ts
    Commit RED state: git commit -m "test(04-01): add failing viewer logic layer tests (VIEW-01, VIEW-02, CONF-03)"
  </action>
  <verify>
    <automated>npx vitest run src/lib/nostr/viewer.test.ts src/lib/blossom/fetch.test.ts 2>&1 | grep -E "FAIL|Cannot find module|failed" | head -10</automated>
  </verify>
  <done>Both test files exist, import from not-yet-created modules, and vitest reports them as FAILED (module not found or test failures) — RED state confirmed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create ThumbnailGrid.test.tsx, Lightbox.test.tsx, useAlbumViewer.test.ts (UI + hook contracts)</name>
  <files>src/components/viewer/ThumbnailGrid.test.tsx, src/components/viewer/Lightbox.test.tsx, src/hooks/useAlbumViewer.test.ts</files>
  <read_first>
    - src/types/album.ts (PhotoEntry — used as test data)
    - src/components/viewer/ThumbnailGrid.test.tsx (does not exist yet)
    - src/components/viewer/Lightbox.test.tsx (does not exist yet)
    - src/hooks/useAlbumViewer.test.ts (does not exist yet)
    - .planning/phases/04-share-link-and-viewer/04-RESEARCH.md (Pattern 7: use-gesture swipe, Pattern 8: JSZip)
  </read_first>
  <behavior>
    ThumbnailGrid.test.tsx must cover:
    - VIEW-03: "renders N skeleton cards when photos array has N entries and no objectUrls loaded"
    - VIEW-06: "calls loadThumbnail(index) when IntersectionObserver fires for that index" — mock IntersectionObserver, fire callback manually, assert prop called with correct index
    - VIEW-03: "renders img elements when objectUrls are provided for photos"

    Lightbox.test.tsx must cover:
    - VIEW-04: "pressing ArrowRight calls onNext" — render Lightbox, fireEvent.keyDown(document, { key: 'ArrowRight' }), assert onNext called
    - VIEW-04: "pressing ArrowLeft calls onPrev" — similar
    - VIEW-07: "pressing Escape calls onClose"
    - VIEW-04: "shows photo counter as '{current+1} / {total}'" — assert text content
    - VIEW-04: "clicking overlay background calls onClose"

    useAlbumViewer.test.ts must cover:
    - VIEW-05: "downloadAll calls JSZip.file() for each photo with photo.filename" — mock JSZip, mock fetchBlob, mock decryptBlob, assert zip.file called N times with correct filenames
    - VIEW-05: "downloadAll reports progress incrementally" — assert onProgress called with (1, N), (2, N), etc.
  </behavior>
  <action>
    Create src/components/viewer/ThumbnailGrid.test.tsx importing from '@/components/viewer/ThumbnailGrid' (does not exist — RED).
    Use @testing-library/react (already installed from Phase 3). Import { render, screen } from '@testing-library/react'.
    Mock IntersectionObserver globally in beforeEach: assign window.IntersectionObserver to a vi.fn() that immediately calls the callback with { isIntersecting: true } for all observed elements.
    Create a sample PhotoEntry array (2 entries) as test fixtures.
    Props interface for ThumbnailGrid: { photos: PhotoEntry[], objectUrls: Record<string, string>, loadThumbnail: (index: number) => void, onPhotoClick: (index: number) => void }.

    Create src/components/viewer/Lightbox.test.tsx importing from '@/components/viewer/Lightbox' (does not exist — RED).
    Props interface for Lightbox: { photos: PhotoEntry[], currentIndex: number, thumbUrls: Record<string, string>, fullUrls: Record<string, string>, albumKey: CryptoKey | null, blossomServer: string, onNext: () => void, onPrev: () => void, onClose: () => void }.
    For keyboard tests: use fireEvent.keyDown(document, { key: 'ArrowRight' }) and assert mock functions called.
    For overlay close: find element with data-testid="lightbox-overlay" and fireEvent.click it.

    Create src/hooks/useAlbumViewer.test.ts importing from '@/hooks/useAlbumViewer' (does not exist — RED).
    Mock 'jszip' with vi.mock: JSZip constructor returns { file: vi.fn(), generateAsync: vi.fn().mockResolvedValue(new Blob()) }.
    Mock '@/lib/blossom/fetch' to return a small ArrayBuffer.
    Mock '@/lib/crypto' decryptBlob to return the input as-is.
    Test that downloadAll calls zip.file(photo.filename, ...) for each photo.

    Run all 3 to confirm RED:
    npx vitest run src/components/viewer/ThumbnailGrid.test.tsx src/components/viewer/Lightbox.test.tsx src/hooks/useAlbumViewer.test.ts 2>&1 | grep -E "FAIL|Cannot find|failed"
    Commit RED state: git commit -m "test(04-01): add failing UI + hook tests (VIEW-03-07, CONF-03)"
  </action>
  <verify>
    <automated>npx vitest run src/components/viewer/ThumbnailGrid.test.tsx src/components/viewer/Lightbox.test.tsx src/hooks/useAlbumViewer.test.ts 2>&1 | grep -E "FAIL|Cannot find module|failed" | head -10</automated>
  </verify>
  <done>All 3 test files exist and are in RED state (module not found or test failures). All 5 Wave 0 test files are committed. Pre-existing tests still pass: npx vitest run --reporter=verbose | grep -E "passed|failed" confirms only the 5 new files are failing.</done>
</task>

</tasks>

<verification>
All 5 Wave 0 test files committed and failing:
- npx vitest run src/lib/nostr/viewer.test.ts — FAIL (module not found)
- npx vitest run src/lib/blossom/fetch.test.ts — FAIL (module not found)
- npx vitest run src/components/viewer/ThumbnailGrid.test.tsx — FAIL (module not found)
- npx vitest run src/components/viewer/Lightbox.test.tsx — FAIL (module not found)
- npx vitest run src/hooks/useAlbumViewer.test.ts — FAIL (module not found)
- Pre-existing test suite (all other tests): GREEN — npx vitest run --reporter=verbose 2>&1 | grep "src/lib\|src/hooks/use[SU]" | grep -v "viewer\|fetch.test\|Lightbox\|Thumbnail\|useAlbum" confirms no regressions
</verification>

<success_criteria>
5 test files exist in RED state. Zero regressions in pre-existing tests. Commit message includes "04-01" prefix. Contracts defined for: decryptManifest, loadAlbumEvent, fetchBlob, ThumbnailGrid, Lightbox, useAlbumViewer.downloadAll.
</success_criteria>

<output>
After completion, create `.planning/phases/04-share-link-and-viewer/04-01-SUMMARY.md`
</output>
